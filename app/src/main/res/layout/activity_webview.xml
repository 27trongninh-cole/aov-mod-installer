<?xml version="1.0" encoding="utf-8"?>
<!-- Root nền trong suốt: chừa khoảng trên cùng để "cửa sổ" bên dưới trông
     giống 1 lớp phủ nổi lên (bo góc trên) thay vì choán trọn màn hình như
     Activity thông thường. Kết hợp hiệu ứng slide_up_in khi mở. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00000000">

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="34dp"
    android:orientation="vertical"
    android:background="@drawable/webview_window_bg"
    android:elevation="12dp"
    android:clipToOutline="true">

    <!-- Top bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#16213e"
        android:paddingStart="8dp"
        android:paddingEnd="16dp"
        android:elevation="4dp">

        <Button
            android:id="@+id/btn_webview_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="←"
            android:textSize="20sp"
            android:textColor="#ffffff"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <TextView
            android:id="@+id/tv_webview_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Map Texture Tool"
            android:textColor="#ffffff"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingStart="12dp" />

        <Button
            android:id="@+id/btn_webview_refresh"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="↻"
            android:textSize="18sp"
            android:textColor="#e94560"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/webview_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:indeterminate="true"
        android:indeterminateTint="#e94560" />

    <!-- WebView -->
    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
</FrameLayout>

